#!/bin/bash

CHANGELOG="/usr/local/share/proxmigrate/CHANGELOG.md"

if [[ ! -f "$CHANGELOG" ]]; then
  echo "❌ CHANGELOG.md nu a fost gasit la $CHANGELOG"
  exit 1
fi

VERSION=$(grep -m1 -oP '\[v[0-9]+\.[0-9]+\.[0-9]+\]' "$CHANGELOG" | tr -d '[]')
DATE=$(grep -m1 -oP '[0-9]{4}-[0-9]{2}-[0-9]{2}' "$CHANGELOG")

echo -e "🛠️ ProxMigrate versiune: \033[1;36m$VERSION\033[0m"
echo -e "🗓️  Ultima modificare:   \033[1;33m$DATE\033[0m"
